export default function styles (theme) {
  return {
    previousClassName: {
      verticalAlign: "middle",
      color: theme.palette.textColor,
      cursor: "pointer"
    },
    nextClassName: {
      verticalAlign: "middle",
      color: theme.palette.textColor,
      cursor: "pointer"
    }
  }
};
